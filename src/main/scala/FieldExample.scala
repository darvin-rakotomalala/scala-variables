/* Scala program of field scope for Scala variable
class created with field variables x and y. */
class FieldExample {
  var x = 10.3f
  var y = 7f

  def printXValue(): Unit = {
    println("Value of x : " + x)
  }

  println("Value of y : " + y);
}
