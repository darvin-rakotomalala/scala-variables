/* Scala program of Method scope for Scala variable
class created with Local variables s1 and s2. */
class LocalVarExample {
  def multiplication(): Unit = {
    // multiple assignments
    var (s1, s2) = (3, 80);
    var s = s1 * s2;
    println("-> Area is: " + s)
  }
}
