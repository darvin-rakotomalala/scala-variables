/* Scala program of Method scope for Scala variable
class created with Method variables s1 and s2. */
class MethodParamExample {
  def multiplication(s1: Int, s2: Int): Unit = {
    var result = s1 * s2
    println("-> Area is: " + result);
  }
}
